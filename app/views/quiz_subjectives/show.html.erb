<% for column in QuizSubjective.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @quiz_subjective.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @quiz_subjective %> |
<%= link_to 'Back', :action => 'list' %>
