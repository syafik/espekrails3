<h1>Editing quiz_objective</h1>

<%= start_form_tag :action => 'update', :id => @quiz_objective %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Edit' %>
<%= end_form_tag %>

<%= link_to 'Show', :action => 'show', :id => @quiz_objective %> |
<%= link_to 'Back', :action => 'list' %>
