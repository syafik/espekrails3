$('#collect_trainer_<%= @trainer_id %>').html('<%= escape_javascript(
                                                        link_to('Tambah',
                                                                trainer_collect_path(@trainer_id),
                                                                :id => "collect_trainer_#{@trainer_id}",
                                                                :remote => true)) %>');